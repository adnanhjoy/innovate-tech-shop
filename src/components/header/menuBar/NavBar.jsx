import React from 'react';
import WebNav from './WebNav';

const NavBar = () => {
    return (
        <div className='shadow-lg'>
            <WebNav />
        </div>
    );
};

export default NavBar;